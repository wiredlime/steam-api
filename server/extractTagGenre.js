/**
 * For making genres and tags
 */
// const genreSet = new Set();
// const tagSet = new Set();
// result.forEach(({ genres, steamspy_tags }) => {
//   console.log(genres, steamspy_tags);
//   genres.forEach((e) => {
//     if (e.length > 1) genreSet.add(e);
//   });
//   [...steamspy_tags].forEach((e) => {
//     if (e.length > 1) tagSet.add(e);
//   });
// });
// const genres = Array.from(genreSet);
// const tags = Array.from(tagSet);

// await db.collection("genres").insert({ genres, count: genres.length });
// await db.collection("steamspy_tags").insert({ tags, count: tags.length });
